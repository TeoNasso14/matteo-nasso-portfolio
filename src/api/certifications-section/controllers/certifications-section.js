'use strict';

/**
 * certifications-section controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::certifications-section.certifications-section');
