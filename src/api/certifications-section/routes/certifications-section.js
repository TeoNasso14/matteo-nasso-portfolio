'use strict';

/**
 * certifications-section router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::certifications-section.certifications-section');
