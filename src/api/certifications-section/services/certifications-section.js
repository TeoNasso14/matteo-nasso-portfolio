'use strict';

/**
 * certifications-section service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::certifications-section.certifications-section');
